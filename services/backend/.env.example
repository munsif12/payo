PORT=4000
MONGO_URI=mongodb://localhost:27018/payo?replicaSet=rs0&directConnection=true
JWT_SECRET=change-me
# Cooling-off before a LOOSENING guardian change (remove guardian / raise ceiling) takes
# effect. Production default is 86400000 (24 h); 0 here so the demo is instant.
GUARDIAN_COOLING_MS=0
