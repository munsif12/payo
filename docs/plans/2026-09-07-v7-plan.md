# PAYO v7 Implementation Plan (AI-first UI + motion)

> Subagent-driven, mobile only. **Spec:** `docs/2026-09-07-payo-v7-ai-first-ui-design.md`.
> **Artboards:** `docs/design/ai-first-v1/*.dc.html` — copy values, never approximate.
> P1 first (shared kit + Home); P2, P3, P4 in parallel on disjoint files; P5 closes.

## Global constraints
Tokens only (no new hex), lucide icons, ≥44 pt targets, existing testIDs preserved, card
contract untouched (parity test), i18n parity, transform/opacity-only animation on the UI
thread, reduced motion honoured, jest + tsc green before every commit. Nothing pushed.

## P1 — Motion foundations + Home (apps/mobile)
- [ ] P1.1 `motion/config.ts` per spec §3 (+ value tests); `usePressScale` → 0.96; remove `Breathe` from the mic.
- [ ] P1.2 `useFirstPaint`, `WordRise`, `Waveform` (metering-driven; `useRecorder` exposes smoothed level), `IconSwap`, `Sheet`, `NavyHead` (+ tests for pure parts).
- [ ] P1.3 Home rebuilt: greet / listening / conversation states, head height, digest + suggestions on the sheet, FAB, composer from keyboard icon, Urdu mirror; choreography once per mount; PIN sheet backdrop per artboard.
- [ ] P1.4 jest + tsc green; testIDs unchanged.

## P2 — Chat cards + composer (apps/mobile/src/components/cards, src/ui/Composer*, PinSheet)
- [ ] P2.1 Restyle receipt, spending, check_in, waiting_approval, approvals, help, recipient/confirmation per `Cards` artboard; IconSwap on state changes; press scale.
- [ ] P2.2 PinSheet: 560 pt, 72 pt keys, backdrop `rgba(13,42,61,.45)`, EASE_OUT open / EASE_IN close.
- [ ] P2.3 jest + tsc + parity green.

## P3 — Wallet, Pay, Send flow (app/(tabs)/wallet, pay, app/send/*, app/success)
- [ ] P3.1 Wallet per artboard (NavyHead + Sheet, quick-actions grid, due bill, activity).
- [ ] P3.2 Pay hub tiles + AI bar; Send identifier / institution (logo rows 56 pt) / amount / success (spoken line, save toggle).
- [ ] P3.3 jest + tsc green.

## P4 — Auth + More screens (app/(auth)/*, app/bills/*, app/card, app/pockets/*, app/requests/*, app/settings, app/(tabs)/more)
- [ ] P4.1 Phone / OTP / PIN per artboards.
- [ ] P4.2 Bills, Card, Pockets (navy total card), Requests, Settings, More per artboards.
- [ ] P4.3 jest + tsc green.

## P5 — Review, QA, docs
- [ ] P5.1 Independent review per phase (motion values, first-paint-only, UI-thread-only, testIDs, tokens); fix loops.
- [ ] P5.2 Simulator acceptance §5.2–5.4 with screenshots + a screen recording in `docs/qa/v7/`.
- [ ] P5.3 QA-REPORT v7, README/DEMO-SCRIPT touch-ups; commit.
