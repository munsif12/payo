# PAYO v5 Implementation Plan (full AI coverage + natural Urdu)

> Subagent-driven: one implementer per phase, independent review per phase, orchestrator
> commits. **Spec (binding):** `docs/2026-09-05-payo-v5-full-ai-coverage-design.md`.
> Phases F1 (backend) and U0 (voice spike) have no dependencies and run first, in parallel.

## Global constraints
Roadmap globals + v2–v4 constraints (paisa ints, standard response shape, pending action →
PIN → execute, kit components/tokens only, ≥44pt, i18n parity, suites green before commit,
nothing pushed). `cards.py` ↔ `cardShapes.ts` field parity is a gate. `pan`/`cvv` never
reach the model or a card.

## U0 — Urdu voice spike (services/ai, docs only)
- [x] U0.1 Script `scripts/voice-spike.py`: 3 sentences × 3 Urdu voices × 2 settings →
  `docs/qa/v5/voice/<voice>-<setting>.mp3` + `INDEX.md` (voice ids, settings, char count).
  Hard cap 600 characters billed. TTS on for this run only. (5 of 6 clips — the cap took
  precedence; only 3 Urdu voices exist in the Cartesia library.)
- [x] U0.2 Owner picks → `CARTESIA_VOICE_UR` + controls in `tts.py`; note the pick in INDEX.md.
  (Pick: Aryan, warm — speed 0.9, emotion calm.)

## F1 — Backend (services/backend)
- [x] F1.1 `GET /transactions?q=` filter + `GET /transactions/:id` (own only) + tests.
- [x] F1.2 `GET /cards/mine` adds `last4`, `maskedPan`; `POST /cards/mine/unfreeze` creates
  pending action `card_unfreeze` (requiresPin, amount 0) with executor; freeze route accepts
  only `{ frozen: true }`; tests (unfreeze needs PIN, wrong PIN rejected, freeze instant).
- [x] F1.3 Roadmap Contract 1 table amended; full suite + typecheck green.

## F2 — AI service: tools, cards, prompt, reliability (services/ai)
- [x] F2.1 Card models for every §4.2 kind; `confirmation.autoOpenPin`.
- [x] F2.2 Tools per §4.1 (`get_card` strips pan/cvv — test asserts they never appear in the
  tool result or card); `spending_summary` compare; `list_transactions` `q`.
- [x] F2.3 Prompt policy §4.3 in EN + UR; intent → tool table; history window (12 turns).
- [x] F2.4 Urdu register rewrite (§5) + banned-word test; Urdu number-to-words in
  `trim_for_tts` + tests.
- [x] F2.5 `tests/test_intent_matrix.py` (EN + UR per §2 row) green; `scripts/ai-smoke.py`.
- [x] F2.6 pytest green.

## F3 — Mobile (apps/mobile)
- [x] F3.1 `cardShapes.ts` for every new kind (parity test vs a JSON export of `cards.py`
  schemas — add `scripts/export-card-schemas.py` in F2 if not present).
- [x] F3.2 Renderers: receipt, spending (bars), account, profile, help, card, statements,
  recipients, bills, billers, telco_chips, pockets, request(s) with Approve/Decline, qr.
- [x] F3.3 `autoOpenPin` → PinSheet immediately; `profile.applied` language → i18n switch.
- [x] F3.4 i18n en + ur; jest + tsc green.

## F4 — Review, QA, docs
- [ ] F4.1 Independent review per phase (F1, F2, F3); fix loops.
- [ ] F4.2 Live smoke `scripts/ai-smoke.py` all PASS; simulator acceptance §7.3 via Argent
  with screenshots `docs/qa/v5/`; voice acceptance §7.4.
- [ ] F4.3 QA-REPORT (v5), DEMO-SCRIPT + README updated; commit.
