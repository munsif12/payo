# PAYO v4 Implementation Plan (hands-free conversation)

> Subagent-driven: one implementer, independent review, orchestrator commits.
> **Spec (binding):** `docs/2026-09-05-payo-v4-handsfree-voice-design.md`.

## Global constraints
Roadmap globals + v2/v3 constraints apply (kit components/tokens only, lucide icons, ≥44pt,
i18n parity, reduced motion, jest + tsc green before commit, nothing pushed). Mobile only —
no backend or AI-service change. `useRecorder`/`useConverse` public shapes stay
backward-compatible for existing callers.

## H1 — Pure logic + tests (apps/mobile/src/voice)
- [ ] H1.1 `loopConfig.ts` constants (spec §2.1 table) + test asserting the values.
- [ ] H1.2 `voiceLoopReducer.ts` + `__tests__/voiceLoopReducer.test.ts` — every transition
  row, silent-turn cap, pause/resume, `sent` while off is a no-op, `stop` reasons.
- [ ] H1.3 `silence.ts` `silenceDecision` + tests (pre-speech timeout, post-speech silence,
  `hadSpeech` latching, max clip).
- [ ] H1.4 `speakSafety.ts` `speakSafetyMs` + tests.

## H2 — Hooks
- [ ] H2.1 `useRecorder`: apply `silenceDecision`; `onFinished({ uri, mime, hadSpeech })`.
- [ ] H2.2 `useConverse`: options `{ onSpeechEnd, onTurnDone, onError }`; drop the 4 s
  timeout for the duration-derived cap; `interrupt()`.
- [ ] H2.3 `PinSheetProvider` exposes `isOpen`.
- [ ] H2.4 `useVoiceLoop.ts` per spec §2.4 (re-arm delay, no-speech → no send, pause on
  PIN, stop on background/blur, `onTextSend`).

## H3 — UI + i18n
- [ ] H3.1 `Composer` prop `listening` → `live`; `VoiceStatusBar.tsx` (4 states, tap to
  interrupt, ≥44pt, a11y label).
- [ ] H3.2 Home wiring: `useVoiceLoop`, status bar, hint line (live / ended), typed send →
  `onTextSend`, suggestion tap keeps the loop live.
- [ ] H3.3 i18n en + ur keys (spec §2.5); parity test green.
- [ ] H3.4 `npx jest` + `tsc --noEmit` green.

## H4 — Review, QA, docs
- [ ] H4.1 Independent review against the spec; fix loop.
- [ ] H4.2 Simulator acceptance (spec §4 items 1–5) via Argent; screenshots `docs/qa/v4/`.
- [ ] H4.3 QA-REPORT (v4 section), DEMO-SCRIPT + README updated; commit.
